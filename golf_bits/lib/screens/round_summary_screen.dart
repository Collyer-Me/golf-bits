import 'package:flutter/material.dart';

/// End of round: standings / settle-up (placeholder).
class RoundSummaryScreen extends StatelessWidget {
  const RoundSummaryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Round summary'),
      ),
      body: const Center(
        child: Text('Summary — add UI here'),
      ),
    );
  }
}
